# PROJECT PLANNER — septet LGMF 2026

> **What this is** (the tuba piece's device, kept): the working view of the PIECE as a
> collapsible outline — SECTION → container → gesture → decisions. Raw ideas land in
> `COMPOSITION_NOTES.md` verbatim first and get folded into a tier here. Engineering
> detail stays in `PLAN.md`. Rewritten freely; the sketch pad is the append-only record.

**NOW ►** 2026-09-24 (session 14, Fable) — **`1s` THE MORPH BETWEEN `min` AND `max` BUILT, VERIFIED, PUSHED (RUNNING_LOG §329; PLAN § `1s`; `morph_panel.js` alone); `1s.6` his one test — reload, no restart; then `1r.6` and his session-13 tests. His call: `min` as a hard floor (§329).** *(Before it:)* **NOW ►** 2026-09-24 (session 14 · CHECKPOINT #1, Opus) — **`1r` BUILT AND PUSHED (`f084f99`); `1r.6` his one test — reload, no restart; then his session-13 tests. Journal §2 SESSION 14 · CHECKPOINT #1 is the cold-start block.** *(Before it:)* **NOW ►** 2026-09-24 (session 14, Fable) — **`1r` CONVERGE ON A TAKE IS BUILT AND VERIFIED at his word *"build here go"* (RUNNING_LOG §325; `morph_panel.js` alone; his one test next — a RELOAD only, no restart). Before it, the design: PLANNED at his word "a" (PLAN § `1r` · RUNNING_LOG §322 · §323): the take is the ARRIVAL — each player opens a semitone away from their partner (the higher up, the lower down — §324) and closes onto their own just note, the vibraphones held still; NOT BUILT — Opus at his word, one commit; `1r.6` his (a reload only). Found on the way: what he liked in `ACT-CONVERGE-01` (`lgmf-s03-convergeA`, his, at 427 s in Sec02) is the STOCK CONVERGE — the take on the pulldown was refused under M3. His session-13 tests still owed (`1q.8` · the overdub · `1q.4` · `1p` · `1n.6` · `1o.6` · `1m.4`).** *(Before it:)* **NOW ►** 2026-09-24 (session 13 CLOSED, Opus) — **HIS TESTS, NOTHING BEING BUILT: `1q.8`, the level first · the percussion overdub (RUNNING_LOG §321 — the menu holds all fourteen; recording only adds) · `1q.4` · `1p` · `1n.6` · `1o.6` · `1m.4`; reload the tab. Journal §2 SESSION 14 OPENS ON THIS is the cold-start block.** *(Before it:)* **NOW ►** 2026-09-24 (session 13 · CHECKPOINT #10, Opus) — **HIS TESTS, NOTHING BEING BUILT: `1q.8` — the take named · the note card · the shuffle (§317) · the volume fix, a struck note keeps its velocity through a take (§319, DYNAMICS_LAW Rule 5) · the dyn box on the strip (§320) — then `1q.4`'s real CTRL+drag · `1p` · `1n.6` · `1o.6`; reload the tab. Journal §2 CHECKPOINT #10 is the cold-start block.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1q.9` THE DYN BOX ON THE STRIP — BUILT AND VERIFIED BY CAPTURE (RUNNING_LOG §320; LG-105): a multi-selection's dynamic set at once through the note card's one rule (`NoteCard.applyLevel`, now under the law: struck by velocity, shaped by the table, full-fader by height); the boxes read the common dynamic back. ► NEXT: `1q.8` HIS ONE TEST (reload the tab) — the level, the dyn box; `1p` · `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **THE VOLUME FIX (RUNNING_LOG §318 · §319; DYNAMICS_LAW Rule 5): his ear caught `1q.1` lifting a STRUCK note to the drawn scale's floor when a take gave it cents; now a struck note keeps its velocity through a take and a shuffle (`velAbs = recVel`, the fader pinned full, its curve channel with the bend), `back` restores it — verified by capture on his `pointilistic01a`. ► NEXT: `1q.8` HIS ONE TEST (reload the tab) — the level first; `1p` · `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1q.5` THE TAKE NAMED · `1q.6` THE NOTE CARD · `1q.7` THE SHUFFLE ARE BUILT AND VERIFIED (RUNNING_LOG §317; from his `1q.4` feedback, §316 · LG-104; one commit): the strip names the take a selection came from, read from the note, the menu's blue its own · the card's line with the cents, the partial and the take, an octave keeping the cents · `shuffle` · `seed` · chips — another partial of the note's own take's series, in range, a seed that reproduces. ► NEXT: `1q.8` HIS ONE TEST (reload the tab); `1p` · `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-24 (session 13 · CHECKPOINT #9, Opus) — **HIS TESTS, NOTHING BEING BUILT: `1q` a take's harmony onto a selection · the marquee · the stack (RUNNING_LOG §312 … §314) · the top bar that grows so the lanes start below it (§315 — the English horn's solo was covered) · `1p` · `1n.6` · `1o.6` — reload the tab. Journal §2 CHECKPOINT #9 is the cold-start block.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1q` A TAKE'S HARMONY ONTO A SELECTION · A MARQUEE · THE STACK IS BUILT END TO END — `1q.1` · `1q.2` · `1q.3`, three commits (RUNNING_LOG §312 · §313 · §314; PLAN § `1q`): the HARMONY STRIP, `take ▾` · `back`; CTRL+drag the marquee, SHIFT+CTRL adds; the stack's cycling verified with real clicks, ALT+click the list on every lane, a trill in the stack. ► NEXT: `1q.4` HIS ONE TEST — reload the tab (journal §2 ►►); `1p` · `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1q.2` THE MARQUEE IS BUILT AND VERIFIED (RUNNING_LOG §313): CTRL+drag on empty lane space selects the bricks the rectangle touches, SHIFT+CTRL adds, ESC cancels; placed before the trill-span branch (SHIFT alone is still the span). The pane cannot hold CTRL in a drag, so the real CTRL+drag is his `1q.4`. ► NEXT: `1q.3` the stack.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1q.1` THE TAKE ONTO A SELECTION IS BUILT AND VERIFIED (RUNNING_LOG §312 · PLAN § `1q`, planned the same day at his word, §306 … §311, LG-103): the HARMONY STRIP at the top right of the score whenever pitched notes or trills are selected — `take ▾` the sequence drawer's takes menu (▸ hears, the name re-pitches each selected note from its own player's note of the take, cents and all; a trill its lower note) · `back` to the pitches before the first take, remembered on the object; `composer.html` one script tag. ► NEXT: BUILD `1q.2` the marquee (CTRL+drag) → `1q.3` the stack; `1q.4` his one test; `1p` · `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-24 (session 13 · CHECKPOINT #8, Opus) — **HIS TESTS, NOTHING BEING BUILT: `1p` the end time and the cursor's clock (a `len | end` menu on every row, the clock after `]`; RUNNING_LOG §303 · §304) · `1n.6` dynamics · `1o.6` the save structure — reload the tab. The panel tabs now sit above the composer's bottom bar (§305). Journal §2 CHECKPOINT #8 is the cold-start block.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1p.1` AMENDED AT HIS WORD after the build (RUNNING_LOG §304): the `len | end` choice is PER ROW — a menu on every row of the orchestration panel beside its box, each instrument choosing for itself; the bar's menu for the column's box alone. Built, verified, THE SHIELD byte-identical. ► NEXT: HIS ONE TEST (reload the tab); `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1p` THE END TIME AND THE CURSOR'S CLOCK IS BUILT AND VERIFIED (RUNNING_LOG §303 · PLAN § `1p` · one commit): `length | end` on the length line — an end on the pattern's clock, the length written behind it, three columns released at one end, the switch a browser preference · the cursor's time after `]`, running while it plays; THE SHIELD byte-identical. ► NEXT: HIS ONE TEST (reload the tab); `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-24 (session 13, Fable) — **`1p` THE END TIME AND THE CURSOR'S CLOCK IS PLANNED at his word (PLAN § `1p` · RUNNING_LOG §302 · LG-102): `1p.1` a `len | end` switch on the length line — an end time on the pattern's clock, the length written behind it, a multi-selection released together, the switch a browser preference · `1p.2` the cursor's time beside `⏮ [ ]`, read-only, running while it plays; THE SHIELD on both. ► NEXT: build it on Opus at his word, one commit for the two; `1n.6` · `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-22 (session 13 · CHECKPOINT #7, Fable) — **`1n` (one-en) DYNAMICS IS BUILT END TO END at his word (RUNNING_LOG §296 … §300; PLAN § `1n`): `1n.1` one scale for short and held (B2, the residual, `texture_dyn.js` the one helper, the cut and the flag) · `1n.2` the `dyn` box and the switch per row, the mark · `1n.3` ranges `flat` and `ramp`, the `dynamics` line, `enter: fade`, the bands · `1n.4` the pointillistic model, five presets · `1n.5` the waves model on the sequence's own generator, `who moves`, one library — THE SHIELD in each. ► NEXT: `1n.6` HIS ONE TEST (reload the tab; `1o.6`'s restart still his) → revise on his word. The SI2 brass DECIDED 2026-09-23, his "b" (§301, D27): a short on a voice with no curve copy takes its set fader on its own channel, no rack change — built and verified through the score's own playback; a moving fader on such a voice still needs a copy.** *(Before it:)* *(1n.1 in one breath:)* PLAN § `1n.1` — the residual beside the table (`dyn_table_check` 68), `texture_dyn.js` the one helper SPACE and Insert read, `sample | follow | auto`, the cut and the flag, THE SHIELD byte-identical. FOUND: the SI2 brass carry a curve copy for `ord` alone — his call. ► NEXT: `1n.2` the single note by hand → 1n.3 → 1n.4 → 1n.5, one commit each; `1n.6` his; `1o.6` still his.** *(Before it:)* **NOW ►** 2026-09-22 (session 13 · CHECKPOINT #6, the build on Fable, the wrap on Opus) — **`1o` (one-oh) THE SAVE STRUCTURE IS BUILT: 1o.1 … 1o.5, five commits, THE SHIELD in each (RUNNING_LOG §291 … §295; PLAN § `1o`) — a pattern a document of its own on disk (`bank/patterns.json`, the FIFTH store), `texture ▾` starts · `pattern ▾` recalls · name · save · revert · • · duplicate · × · new · Insert the sequence's idiom. ► NEXT: `1o.6` HIS ONE TEST — RESTART the server first, then reload → revise on his word → BUILD `1n dynamics` on Opus after a checkpoint + clear.** *(Before it:)* **NOW ►** 2026-09-22 (session 13, Fable) — **`1o.1 the document` BUILT (RUNNING_LOG §291; PLAN § `1o` — a texture pattern a DOCUMENT OF ITS OWN carrying a copy of its onsets, `txPat()` the open one, the "starts again" reset gone, a pre-1o.1 pattern upgraded in place; every named check run, THE SHIELD byte-identical). ► NEXT: `1o.2 starting and recalling` → 1o.3 (restart the server after it) → 1o.4 → 1o.5, one commit each; 1o.6 his → then `1n dynamics`. His one test of `1m.4` (§284 · §285) still owed.** *(Before it:)* **NOW ►** 2026-09-22 (session 13, Fable) — **`1m.4 articulation` IS BUILT: 1m.4.1 · .3 · .4 · .5 · .6, five commits, THE SHIELD in each (RUNNING_LOG §279 … §283; PLAN § `1m.4`); `1m.4.2` at his rack (26 pending), `1m.4.7` on his word. ► NEXT: HIS ONE TEST (reload the tab) → revise if needed → BUILD `1n dynamics` on Opus after a checkpoint + clear → then `1o`.** *(Before it:)* **NOW ►** 2026-09-22 (session 13, Fable) — **ALL THREE PLANS ARE DESIGNED AND WRITTEN, NOTHING BUILT: `1m.4 articulation` · `1n dynamics` · `1o the save structure` (PLAN § `1o`, 1o.1 … 1o.6 — a pattern a named document with a copy of its onsets · `texture ▾` starts, `pattern ▾` recalls · `bank/patterns.json` a fifth store on the sequence library's rules · Insert the sequence's idiom, the range written; RUNNING_LOG §276 … §278; his decisions §277 — the two rules on one player's notes went into `1n.1`). ► NEXT: checkpoint on Opus + clear → BUILD `1m.4 articulation` on Opus, one commit per step, THE SHIELD in each, his one test after → then `1n` → then `1o`.** *(Before it:)* **NOW ►** 2026-09-22 (session 13 · CHECKPOINT #2, Opus) — **TWO OF THE THREE PLANS ARE DESIGNED AND WRITTEN, NOTHING BUILT: `1m.4 articulation` (PLAN § `1m.4`) and `1n dynamics` (PLAN § `1n`). ► NEXT: `/postclear` on FABLE → VET `1o the save structure`, plan 3 of 3 — phase 1, HIS WORDS FIRST (LG-84 · LG-85: the orchestrated pattern as a named document carrying a copy of its onsets, several per texture, where it lives, a library, how Insert reads it — and now the ranges and `dyn` values of `1n`). THEN the builds, one per clear on Opus, `1m.4` first, his one test after each.** *(Before it:)* **NOW ►** 2026-09-22 (session 13, Fable) — **`1n dynamics` PLANNED IN FULL AND WRITTEN (PLAN § `1n`, six steps 1n.1 … 1n.6; RUNNING_LOG §262 … §275, LG-86 … LG-99): one scale for short and held (B2 — the ladder velocity plus a fader set once from a residual table) · a held note `sample | follow | auto` at one written step · one typed `dyn` box per row · ranges = the selection, `low` · `high` · `who` · `model` · `enter` · generate, levels absolute, a generate overwrites · four models: flat · ramp · pointillistic (three dials in words, five presets) · waves (the sequence's, `who moves`) · the mod wheel untouched · `1f` stays its own `todo` · THE SHIELD on every step. Nothing built. ► NEXT: checkpoint + clear → VET `1o the save structure` on Fable (LG-84 · LG-85) → then the builds, one per clear, `1m.4` first, on Opus.** *(Before it:)* **NOW ►** 2026-09-22 (session 13 · CHECKPOINT #1, Fable) — **HIS ORDER: DESIGN EVERY PLAN NOW, BUILD AND TEST ONE AT A TIME LATER. `1m.4 articulation` is WRITTEN, NOT BUILT (PLAN § `1m.4`; its last bullet is the build order — open it when it is time to build, on Opus). ► NEXT, after `/postclear` on Fable: VET `1n dynamics` (hairpins and crescendos, `1f` folded in) — his words first; then vet `1o the save structure`; then the builds, `1m.4` first, his one test after each.** *(Before it:)* **NOW ►** 2026-09-22 (session 13, Fable) — **`1m.4 articulation` PLANNED IN FULL AND WRITTEN (PLAN § `1m.4`, RUNNING_LOG §240 … §260, LG-74 … LG-85): every voice as data · the by-key menus (the percussion the same) · THE LENS (the panel edits the selection; the one rule for a take) · the column a passive indicator · a stretch by SHIFT+click · `spread` written, not built · THE SHIELD on every step. His tests of `1m.1` … `1m.3` closed at his word; ONE test after the build. ► NEXT: checkpoint + clear → BUILD on Opus, one commit per step → his test → vet `1n dynamics` on Fable, then `1o the save structure`.** *(Before it:)* **NOW ►** 2026-09-21 (session 12 closed, Opus) — **SESSION 12 CLOSED. SESSION 13 OPENS ON TYPES OF ARTICULATION in the texture take (his word) — `1m`'s scenario item (3), laid out with him, Fable (journal §2 · RUNNING_LOG §239).** His tests of `1m.1` · `1m.2` · `1m.3` outstanding (reload). *(Before it:)* 2026-09-21 (session 12, Fable) — **`1m.3` DURATION BUILT AND VERIFIED with no MIDI — HIS TEST OUTSTANDING (reload; RUNNING_LOG §237 · §238 — his first test found the double-click's box never came, no click follows a press on a re-drawn circle; the second press now opens it): the standard short 120 ms in the bar, a length per column in seconds, a player's own by a double-click on their circle, the bars drawn; `hear: strike` follows the short on a texture take.** ► NEXT, with him: Insert · the percussion row · dynamics · re-attack. *(Before it:)* 2026-09-21 (checkpoint #4, Fable) — **`1m.3` DURATION agreed and written (RUNNING_LOG §234 … §236): the standard short 120 ms, a length per column, an override per player, bars drawn. ► NEXT: BUILD `1m.3` after `/postclear`, on his go.** *(Before it:)* 2026-09-21 (session 12, Fable) — **`1m.2` THE COLUMNS, THIRD FORM, BUILT AND VERIFIED with no MIDI — HIS TEST OUTSTANDING (reload; RUNNING_LOG §233): a column's players ARE its ticks, tick = on = dealt = sounds, no order of operations; the drawer a view of one column, the others dealt offline.** The triangle is track 10 in his rack (§221), his to mute. ► NEXT, with him: Insert · the percussion row · dynamics · re-attack. *(Before it:)* 2026-09-21 (session 12, Fable) — **`1m.2` THE COLUMNS BUILT AND VERIFIED with no MIDI, HIS TEST OUTSTANDING (reload the tab; RUNNING_LOG §227): a column under every ON mark, a drawer TAKE linked and recalled, the circles the ticks, SPACE the rhythm preview.** Before it, his first test of `1m.1` reshaped the row (§221 … §223) and found the triangle: track 10 `Percussion` in his rack, his to mute. ► NEXT, laid out with him: Insert · the percussion row · dynamics · re-attack. *(Before it:)* 2026-09-21 (session 12 · a new direction, Fable) — **`1m`, A TEXTURE TAKE IN THE STRIKES DRAWER — his verdict on `1l` in use: *"this tool is not working the way I expected it"*; built ONE STEP AT A TIME. `1m.1` THE SWITCH AND THE TOP ROW IS BUILT AND VERIFIED with no MIDI — HIS TEST OUTSTANDING (reload the tab): `source [the strike | a texture take]`, a rhythm take as a row of dots, all off, clicked on, SPACE on the claves, a range, a cursor, ALT + wheel at the pointer.** The percussion: the bytes are right (`LGPerc` ch 7 key 41); the rack file shows a `Percussion` track on ALL channels, armed and unmuted — his to mute and listen. ► NEXT, laid out with him: the columns (RUNNING_LOG §214 … §220 · LG-63 … LG-66 · PLAN § `1m`). *(Before it:)* 2026-09-21 (session 12 · his listen, Opus) — **`1l.8` IN PROGRESS: HE IS COMPOSING WITH THE RHYTHM PANEL (`LGMF-Rseq-01`, a 240 s harmony row on the silver ratio, the bellows).** Fixed at his word "now": the percussion's fourteen instruments · a dot moved to or doubled on other players (RUNNING_LOG §212 · §213). Collected for later: triangles for claves · Texture resizable · SPACE in Texture (§207). Multitempo into Texture: planning phase 1 closed, the top line next (LG-61). *(Before it:)* 2026-09-21 (session 12 · the build, Opus) — **`1l` BUILT END TO END (`1l.1` … `1l.7`, RUNNING_LOG §199–§206); ► NEXT: `1l.8`, HIS LISTEN — restart the server, reload, write a short counterpoint passage end to end.** *(Before it:)* 2026-09-21 (session 12, Fable) — **`1l`, THE COUNTERPOINT SECTION, IS PLANNED IN FULL — EIGHT STEPS, EACH AGREED BY HIM, NOTHING BUILT (`docs/PLAN.md` § `1l`, `1l.1` … `1l.8` · RUNNING_LOG §185–§197 · COMPOSITION_NOTES LG-56 … LG-60).** The rhythm machine is the TEXTURE panel (not `MT`): it makes RHYTHM TAKES, as the strikes drawer makes harmony takes · a CLONE of the sequence drawer — the RHYTHM SEQUENCE PANEL — carries a SILENT harmony row (the map: pitch + the intended written dynamic) and a RHYTHM ROW of boxes on top, each an excerpt of a rhythm take · every dot reads ITS player's pitch and level from whatever lies beneath it · a workshop before the box (see · hear in a harmony · cut, or loop the whole take into N boxes) · the dots touched after it (`mute` | `edit`) · two crossfades, scattered or player by player. **► NEXT: BUILD `1l.1`, TEXTURE PLAYS THIS ENSEMBLE — Opus, after a checkpoint and a clear.** *(Before it:)* 2026-09-21 (session 11 closed, Opus) — **SECTION 1 IS COMPOSED (`piece-LGMF-Sec01-v1.3-sec01-done`). SESSION 12 OPENS ON `1l`, THE COUNTERPOINT SECTION — a discussion first (his brief LG-55, after LG-53): a sequence underneath as the harmony and the dynamics, rhythms from the multitempo machine on top, every onset reading its pitch and dynamic from the sequence. `1k` `todo`: the morph's peaks stand well above the sequence — look before the next morph.**

*Before it —* 2026-09-20 (session 11, Fable → Opus) — **► CHECKPOINT #8 (journal §2): `1i` (the vibraphones in the bloom) AND `1j` (the morph's breaths) ARE BUILT, VERIFIED, PUSHED — AND HE IS COMPOSING WITH BOTH (he restarted the server; `ACT-BLOOM-05` · `-06` are right). PENDING HIM: the repair of `ACT-BLOOM-03` · `-04` (filed with bending vibraphones by the stale server) — (a) the AI re-renders them in place, (b) he re-files — and his listens.** *(Before it:)* 2026-09-20 (session 11, Fable) — **► PLAN `1j`, THE MORPH'S BREATHS, IS BUILT (RUNNING_LOG §181) — BR4, HIS LISTEN, IS LEFT, AND FIRST HE MUST RESTART THE `:5400` SERVER: it files actuals with the engine it started with (his `ACT-BLOOM-03` · `-04` have bending vibraphones; fixed in `score/server.js`, effective from the restart; the repair of the two is put to him). Journal §2's ► row.** *(Before it:)* 2026-09-20 (session 11, Fable) — **► PLAN `1i`, THE VIBRAPHONES IN THE BLOOM, IS BUILT END TO END (RUNNING_LOG §175 · §176) — WHAT IS LEFT IS VB6, HIS LISTEN (journal §2's ► row). He must reload his tab; his take `Bloom01b_w_vibs-Just-A1-seed132` is the one to try.** *(As it stood when planned:)* 2026-09-20 (session 11, Fable) — **► PLAN `1i`, THE VIBRAPHONES IN THE BLOOM, IS PLANNED AND APPROVED, NOT BUILT (journal §2's ► `1i` row · RUNNING_LOG §172–§174 · LG-54): a fourth pair of a bloom on a take — as assigned, one note each, held still, following the bloom's one shape. NEXT: BUILD `docs/PLAN.md` § `1i`, VB1 → VB5 (Opus). `1h` H4, his listen, is still open.** *(Before it:)* 2026-09-20 (session 11, Opus → Fable → Opus) — **► CHECKPOINT #7 (journal §2): PLAN `1h`, THE BLOOM ON A TAKE, IS BUILT END TO END — H1 · H2 (+ H2.6, the actuals) · H3 — and every named check of its REQUIRED VERIFICATION has run (RUNNING_LOG §169 · §170 · §171; four commits `7f5d178` … `c52e4c0`, all pushed). **WHAT IS LEFT IS `H4`, HIS LISTEN, AND IT IS HIS — nothing in `1h` has been HEARD (no Web MIDI in the in-app browser), and HE MUST RELOAD HIS TAB FIRST: his `:5400` server was live all through the build.** A take from the strikes drawer is the first group of the morph's PITCHES pulldown, frozen at choosing and read AS ASSIGNED (each pair on its just pitch, cents kept, the line showing note · partial · cents); the actuals keep those pitches through recall → vary → save → insert; and the new `score/public/morph_dyn.js` puts every sustained morph note on the dynamics law — mf strike for its own pitch, a curve channel, the fader between its own two written dynamics, the fades a separate weight on top. `morph.js`, `morph_septet.js`, `strike_drawer.js` and `sequence_ui.js` are untouched.** *(The item as it was planned:)* **► CHECKPOINT #6: PLAN `1h`, THE BLOOM ON A TAKE, WAS PLANNED IN FULL AND APPROVED, AND THE BUILD WAS NEXT — on Opus, H1 → H2 (+ H2.6, the actuals keep the pitches) → H3, one step, one verification, one commit each. Nothing was built this sitting; `score/` is untouched (RUNNING_LOG §162–§168).** *(The item:)* **► PLAN `1h`, THE BLOOM ON A TAKE, IS PLANNED AND APPROVED (RUNNING_LOG §162–§168) — four steps: a TAKE from the strikes drawer in the morph's PITCHES pulldown · read AS ASSIGNED, each pair on its JUST pitch, partial + cents shown · the morph's dynamics on the law (every sustained note shaped — one scale with the sequence) · his listen. NOTHING BUILT. NEXT: the BUILD, on Opus, after a checkpoint and a clear.** *(Before it:)* **► HIS TESTS ARE CLOSED AT HIS WORD (RUNNING_LOG §162); THE WORK IN HAND IS THE MORPH PANEL, IN SMALL BUILDS, ONE MODEL AT A TIME — first a BLOOM (M1) on a TAKE from the strikes drawer, chosen in the morph's PITCHES pulldown, with the morph's dynamics (PLAN 1e V5, as amended by the table) built in the same build. Planning method, phase 1: how a take's pitches reach the pairs. Nothing built.** *(Before it:)* **► CHECKPOINT #5 (journal §2): `1g` (ONE SCALE) and THE TAKES MENU are built and pushed (RUNNING_LOG §156–§159); NEXT IS HIS — reload the tab, record the five-box sequence in the rack, and it is read back with `cc7_by_channel.lua`. HE HAS BEGUN COMPOSING: `scores/piece-LGMF-Sec01` + `-v1.1-1stSequence` (his, untracked — asked whether to commit).** **► PLAN `1g` BUILT (§156–§158) — in a sequence, ONE scale: every sustained note is shaped (mf strike, curve channel, the fader at its table value), so a straight `pp` box IS the waves' `low`; his ear had found the line into a straight box a +10 … +18 dB step. HIS RACK TEST OUTSTANDING.** *(Before it:)* **► THE SEQUENCE FEATURE ADD IS BUILT, END TO END, AND NONE OF IT HAS BEEN HEARD (RUNNING_LOG §150–§155).** At his word (*"try to move through the whole plan independently, the whole build, please. And I'll test at the end."*): **1d.10** the dynamics table · **1d.11** the library (`bank/sequences.json`, a store of its own) · **1d.12** a RANGE of boxes with a waves range of its own · **1d.13** the waves by PRESET · **1d.9 + 1d.14** `of max` · `outlier` · `±` in seconds · **1d.15** the clock and the cursor — each verified in the running app with no MIDI, each journalled and committed on its own. `sequence_check` **126 → 180**, `dyn_table_check` **51** and `test_snapshots` **26** are new, every other battery green. **► WHAT IS LEFT IS HIS EAR:** the test at the foot of each item, and **1d.6, the listen** — in his Chrome, because the in-app browser has no Web MIDI. *(What follows is the line as it stood when only 1d.10 was built.)* **1d.10, THE DYNAMICS TABLE (RUNNING_LOG §150): every written dynamic now has a CC7 value of its own, 4 dB a step through each instrument's MEASURED fader curve** — cello `ppp … fff` = 43 · 51 · 59 · 69 · 81 · 94 · 109 · 127, bassoon 24 · 32 · 40 · 50 · 62 · 79 · 100 · 127: two CC7 ladders, the same dB, and `mp → ff` = 69 → 109, his own *"say 65 to 111"*. Where 1e gave every shape 0 … 127 and only its DEPTH was heard, `ppp–mp` is now 43…69 against `pp–mf` 51…81. `score/public/dyn_table.js` · `dyn_table_check` **51** · `sequence_check` still **126** · `DYNAMICS_LAW.md` §3 rewritten, its banner gone. **► NEXT: his rack test of 1d.10, and 1d.11 THE LIBRARY** (`bank/sequences.json`, a file of its own) → 1d.12 select a range → 1d.13 the waves by preset → 1d.9 + 1d.14 the breath's lengths → 1d.15 the clock and the cursor → 1d.6 his listen. **And `1f`, `todo`: the crescendo tool was never brought under 1e.** *(What follows is the line as it stood when the feature add was planned and nothing was built.)* `docs/PLAN.md` § 1d, THE FEATURE ADD — planned and approved, RUNNING_LOG §145–§148 · LG-50 · LG-51. Opus, after a checkpoint and a clear. *(What follows was the NOW line before the design talk.)*

*Earlier the same day —* **► PLAN 1e IS DONE AND PROVEN IN HIS RACK (RUNNING_LOG §143 · §144).**
He recorded the playback and `cc7_by_channel.lua` read it back: **MAIN ch 1 empty on all eighteen tracks** · every shaped note on a
**curve channel**, three per player in rotation, the SI2 three on their `b` ports · **every strike an mf velocity** for its own pitch,
checked against the bank · **CC7 reaching 0** on the first channel of every player, the floor of 56 elsewhere being `ppp` under an `mf`
top to the digit. Where §140 found every note struck at 127 with CC7 moving 63 … 127 **on MAIN**, the rack now receives every note at
its own mf velocity with CC7 moving **0 … 127 on the curve channels**. The law is `docs/DYNAMICS_LAW.md`, the first read for any
sound-path work. **► NEXT: the sequence FEATURE ADD comes off deck — `docs/PLAN.md` § 1d and 1d.9, with a short design talk owed
first (four questions, all his, in journal §2).** *What follows is the item as it was written to be built:*
**BUILD PLAN 1e, THE VOLUME FIX: a note whose volume is SHAPED takes THE NORMALIZED FADER (CC7 0 → 127, `cc7Abs`), struck at MF (`velAbs`), on the CURVE CHANNELS.** *Why:* his ear, twice — *"it sounds Like the whole sequence is just sitting at the high dynamic. No waves."* Two bugs were found and fixed on the way (§137 Hear streams the fader on MAIN ch 1; **§139** `insert()` never dropped the CACHED curve-channel map, so an inserted sequence fell back to MAIN once the score had been played — §75's bug by another door), and then **his recording, read back per track and channel** (`reaper/bridge/jobs/cc7_by_channel.lua`, new; RUNNING_LOG **§140**) showed the routing right and the real fault: every shaped note struck at the velocity of its shape's TOP with CC7 moving 63 … 127 — about **12 dB, at the timbre of the top**. **HIS DIAGNOSIS closed it (§141):** *"in the tuba piece and in the last piece, we always made crescendos from zero … CC7 zero to CC7 max … a normalized one."* **And the machinery is ALREADY IN THE SCORE, per note, built by piece #5 eleven days ago for this same problem** (`wc.cc7Abs {lo,hi}` bypasses the 12 dB ladder; `wc.velAbs` sets the strike) — **so nothing in `composer.html` changes; the fix is what the TOOLS WRITE.** `docs/PLAN.md` § **1e** (V1 … V7) is written to be executed cold: **V1** the sequence's Insert · **V2** its Hear, ramped notes onto a curve route · **V3** one `curveDirty()` line in the strikes drawer's and the morph panel's inserts · **V4** `swell_ui.js` + a `full fader` checkbox on the note card · **V5** the morph — NOTHING BUILT, its method written into `MORPH_NOTES.md` for its revision · **V6** one check, no probes · **V7** `docs/DYNAMICS_LAW.md`. **Decided: mf for ALL seven** (the level lost against a struck fff is 3.9 … 5.4 dB, uniform since 1b, so the balance holds). **Step 1 of his top line found nothing to fix:** the trills carry volume as separate STRUCK notes by velocity, the septet's design. **`sequence.js` is not touched — `sequence_check` stays 126.** *Before it, the same session:* **the sequence panel FLOATS** (dragged by the head, sized by the corner, its place remembered) and every font in it is up 4 from one constant `FS = 15`, all 41 widths px → em (RUNNING_LOG §132; three bugs of the AI's own on the way — a hidden element measures 0 × 0 · `parseFloat('0px') || was` · a `ResizeObserver` with no reference is collected). **ON DECK, DEFERRED at his word:** the sequence FEATURE ADD — `docs/PLAN.md` § 1d, THE NEXT FEATURE ADD (a clock · click-to-cursor · `±` in seconds · a sequence LIBRARY · a RANGE of boxes · the waves by PRESET) and **1d.9** (`of max` · `outlier`), with a short design talk owed first. **PLAN 1d is built to its end; his tests of 1d.7 · 1d.8 and his listen (1d.6) are better done AFTER 1e, which changes how they sound.** PLAN 1a · 1b · 1c are closed or built. The LGMF call is still unread at his word.

## The piece — outline (v0 — folded from LG-1 … LG-8 by the AI; **his to confirm, none of it discussed yet**)

**Forces:** english horn + bassoon · horn + trumpet · cello + double bass · percussion (D1).
**Duration cap:** unknown (the call is unread).

**Character (LG-2):** delicate, quiet; the interest in texture and timbre. A counterpoint of
timbres, after Sciarrino (LG-7). Reference to study: Sciarrino, *Raffigurar Narciso al fonte* (LG-8).

**Form (LG-6): a rondo whose refrain is a MORPH.**

1. **Opening.** Two notes pull two ways — *his to settle:* LG-1 says *"start with morph
   section"*; LG-6 says *"a bespoke section beginning, then a morph."*
2. **MORPH (the refrain).** A morph that travels TO a beating pattern and HOLDS; sparse
   figures played inside the hold that still evoke the beating (LG-8).
3. **Bespoke episode.** Candidate material: the pointillistic multitempo / phase-shifted
   section — quiet, many rests, each player in their own pulsed tempo shown by the bouncing
   balls (LG-4, LG-5).
4. **MORPH.**
5. **A different bespoke episode.** Candidate material: patterns from the pattern tool,
   orchestrated by weighting and thinned by algorithm, the harmony changing on a clock with
   codified transitions (LG-7).
6. **MORPH** … *(the rest: open)*

**Across the piece:** animated conductions for the delicate, quiet material — the first
gesture: pinch → slight lift → open (LG-3).

## Open musical questions (for the composer — none urgent, none blocks the port)

- **"Continuous, not sparse" (LG-2) and "lots of rests … sparse pointillistic" (LG-4, LG-5,
  LG-8).** Both are his words, two days apart. Is the rondo the answer — a continuous morph
  refrain, sparse episodes — or did the second thought replace the first?
- **The opening:** a morph (LG-1) or a bespoke section (LG-6)?
- **The pairs:** are they the morph's beating pairs (one pair of players on one pitch — the
  beating tool's own object), or a timbral grouping, or both?
- **Percussion:** which instruments — and is the percussionist the seventh voice of the
  counterpoint of timbres, or the one who marks the form?
- **Who inherits the piano's role?** In piece #5 the piano was the STRUCK voice — the recorded
  strikes, the strike at the end of a crescendo, the harmonics at a morph's re-breaths, the
  articulation lines. This piece has no piano, so those tools arrive quiet (the port's P2).
  Does the percussionist take that role, or does it simply not exist here?
